schema_version: "1.0"

name: "anchor-idl-v0-to-v1"
version: "0.1.14"
description: "Migrate Anchor projects from v0.29 to v0.30: IDL JSON files (v0 to v1 format) and Rust source files (bumps, Cargo.toml, ProgramResult, CLOSED_ACCOUNT_DISCRIMINATOR). Handles all 12 IDL transform rules plus 5 Rust breaking-change patterns."
author: "Jennivarl"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["json", "rust"]

keywords:
  - "anchor"
  - "solana"
  - "idl"
  - "anchor-idl"
  - "migration"
  - "v0-to-v1"
  - "discriminator"
  - "borsh"
  - "rust"
  - "cargo"
  - "0.29-to-0.30"
registry:
  access: "public"
  visibility: "public"

capabilities:
  - "fs"